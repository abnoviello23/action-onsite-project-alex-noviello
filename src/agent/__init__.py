"""Permissioned retrieval agent: orchestrator loop, walkers, Messages client.

Read-only. Nothing here writes graph rows or calls a source API; answers cite
native source ids so an external MCP can act on them.
"""
